{
  "compilerOptions": {
    "baseUrl": ".", // Set the base URL to the root of the project
    "paths": {
      "@my-cms/*": ["my-cms/*"], // Map '@lib' to the 'lib' directory in the root folder

      "@lib/*": ["frontend/lib/*"], // Map '@lib' to the 'lib' directory in the root folder
      "@common/*": ["common/*"] // Map '@lib' to the 'lib' directory in the root folder
    }
  }
}
